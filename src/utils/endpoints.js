const URL_BASE = "http://127.0.0.1:8000";

const URL_LOGIN = `${URL_BASE}/api/login`;
const URL_CHECK_USERS = `${URL_BASE}/api/checkusers`;
const URL_REGISTER = `${URL_BASE}/api/register`;
const URL_RESET_PASSWORD = `${URL_BASE}/api/resetpass`;
const URL_QUINQUENIUM = `${URL_BASE}/api/axe/`;
const URL_PROGRAM_ADD = `${URL_BASE}/api/checkusers`;
const URL_ASK_PASS_RESET = `${URL_BASE}/api/askpass`;
const URL_SUPERUSER_PASSWORD = `${URL_BASE}/api/superuser`;
const URL_GET_ORGAOS = `${URL_BASE}/api/orgs/`;
const URL_ALL_USERS = `${URL_BASE}/api/users/`;
const URL_USER = `${URL_BASE}/api/users/`;
const URL_DEFINITION = `${URL_BASE}/api/duration`;
const URL_POLICY = `${URL_BASE}/api/policy/`;
const URL_PROGRAM = `${URL_BASE}/api/program/`;
const URL_ACTIVITY = `${URL_BASE}/api/activity/`;
const URL_SEASON = `${URL_BASE}/api/season/`;
const URL_GOAlS = `${URL_BASE}/api/goals/`;
const URL_INDICATOR = `${URL_BASE}/api/indicator/`;
const URL_GOALYEARLY= `${URL_BASE}/api/goalyearly/`;
const URL_TASK = `${URL_BASE}/api/task/`;
const URL_FILTER = `${URL_BASE}/api/program/filter/`;
const URL_PROGRAM_GOALS = `${URL_BASE}/api/goals/program_id/`;
const URL_GOALS_INDICATOR = `${URL_BASE}/api/indicator/goal_id/`;
const URL_INDICATOR_ACTIVITY = `${URL_BASE}/api/activity/indicator_id/`;

export {
    URL_LOGIN,
    URL_CHECK_USERS,
    URL_REGISTER,
    URL_RESET_PASSWORD,
    URL_QUINQUENIUM,
    URL_PROGRAM_ADD,
    URL_ASK_PASS_RESET,
    URL_SUPERUSER_PASSWORD,
    URL_GET_ORGAOS,
    URL_ALL_USERS,
    URL_USER,
    URL_DEFINITION,
    URL_POLICY,
    URL_PROGRAM,
    URL_ACTIVITY,
    URL_SEASON,
    URL_GOAlS,
    URL_INDICATOR,
    URL_GOALYEARLY,
    URL_TASK,
    URL_FILTER,
    URL_PROGRAM_GOALS,
    URL_GOALS_INDICATOR,
    URL_INDICATOR_ACTIVITY
}